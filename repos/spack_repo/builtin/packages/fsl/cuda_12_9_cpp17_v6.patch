--- a/src/eddy/Makefile
+++ b/src/eddy/Makefile
@@ -57,6 +57,6 @@
 	CUDACXXFLAGS=-DCOMPILE_GPU
 	CUDAOBJS=CBFKernelDefinitions.o CBFSparseDiagonalMatrix.o CBFSplineField.o LSResampler_cuda.o DiffusionGP_cuda.o PostEddyCF_cuda.o EddyGpuUtils.o EddyInternalGpuUtils.o CudaVolume.o EddyMatrixKernels.o EddyKernels.o GpuPredictorChunk.o StackResampler.o DerivativeCalculator.o EddyCudaHelperFunctions.o
-	CUDALDFLAGS= -Xlinker -rpath $(LIB_CUDA) -L$(LIB_CUDA) -lcublas -lcudart
+	CUDALDFLAGS= -Wl,-rpath,$(LIB_CUDA) -L$(LIB_CUDA) -lcublas -lcudart
 	ifeq ($(fastbuild),1)
 		GENCODE_FLAGS    := -gencode arch=compute_30,code=sm_30
 	endif
@@ -90,7 +90,7 @@
 ifdef NVCC11
 	NVCC=${NVCC11}
 endif
-NVCCFLAGS=-c -DCOMPILE_GPU -O3 -m 64 -std=c++11
+NVCCFLAGS=-c -DCOMPILE_GPU -O3 -m 64 -std=c++17 -DFSL_CUDA_HIDE_ROUND
 ifeq ($(CLOBBER_CLANG),1)
    NVCCFLAGS+= -DCLOBBER_CLANG
 endif
--- a/src/utils/options.h
+++ b/src/utils/options.h
@@ -602,8 +602,7 @@
        @return true on success.
     */
     unsigned int parse_option(const std::string& optstr, const std::string& valstr,
-			      char *argv[], int valpos, int argc)
-      throw(X_OptionError);
+			      char *argv[], int valpos, int argc);
 
     /**
        @param str A string of the form --option[=value].
--- a/src/utils/parse.cc
+++ b/src/utils/parse.cc
@@ -81,7 +81,6 @@
 
   unsigned int OptionParser::parse_option(const string& optstr, const string& valstr,
 					  char *argv[], int valpos, int argc)
-    throw(X_OptionError)
   {
     BaseOption * theOption = 0;
 
--- a/src/miscmaths/miscmaths.h
+++ b/src/miscmaths/miscmaths.h
@@ -140,9 +140,11 @@
 
   // General maths
 
+#ifndef FSL_CUDA_HIDE_ROUND
   int round(int x);
   int round(float x);
   int round(double x);
+#endif
   double rounddouble(double x);
 
   inline int sign(int x){ if (x>0) return 1; else { if (x<0) return -1; else return 0; } }
--- a/src/newimage/newimagefns.h
+++ b/src/newimage/newimagefns.h
@@ -1986,7 +1986,7 @@
 	      for (vector<offset>::iterator it=neighbours.begin();it!=neighbours.end();it++) {
 		int xnew(x+it->x),ynew(y+it->y),znew(z+it->z);
 		if ( (xnew>=vol.minx()) && (ynew>=vol.miny()) && (znew>=vol.minz())
- 		     && (MISCMATHS::round(vol(xnew,ynew,znew)))==val) {
+ 		     && (static_cast<int>(::round(vol(xnew,ynew,znew))))==val) {
 		  // Binary relation
 		  int lval2 = labelvol(xnew,ynew,znew);
 		  if (lval != lval2) {
--- a/src/fdt/Makefile
+++ b/src/fdt/Makefile
@@ -171,5 +171,6 @@
 
 ${LIBBEDPOSTX_CUDA}:
-		${NVCC} --shared --compiler-options '-fPIC' -o CUDA/libbedpostx_cuda.so CUDA/init_gpu.cu CUDA/samples.cu CUDA/diffmodels.cu CUDA/runmcmc.cu  CUDA/xfibres_gpu.cu -O3 ${GENCODE_FLAGS} -lcudart -lcuda -lcurand -I. -L${LIB_CUDA} -L${LIB_CUDA}/stubs -ICUDA/options -I${INC_NEWMAT} -I${FSLDIR}/include -I${INC_BOOST} -I${INC_CUDA} -maxrregcount=64
+		${RM} -f CUDA/libbedpostx_cuda.so ${FSLDEVDIR}/lib/libbedpostx_cuda.so
+		${NVCC} --shared --compiler-options '-fPIC' -std=c++17 -DFSL_CUDA_HIDE_ROUND -o CUDA/libbedpostx_cuda.so CUDA/init_gpu.cu CUDA/samples.cu CUDA/diffmodels.cu CUDA/runmcmc.cu CUDA/xfibres_gpu.cu -O3 ${GENCODE_FLAGS} -lcudart -lcuda -lcurand -I. -L${LIB_CUDA} -L${LIB_CUDA}/stubs -ICUDA/options -I${INC_NEWMAT} -I${FSLDIR}/include -I${INC_BOOST} -I${INC_CUDA} -maxrregcount=64
		@if [ ! -d ${FSLDEVDIR}/lib/ ] ; then ${MKDIR} ${FSLDEVDIR}/lib ; fi
		${CP} -rf CUDA/libbedpostx_cuda.so ${FSLDEVDIR}/lib
@@ -183,4 +184,4 @@
 		   ${CXX} ${CXXFLAGS} ${LDFLAGS} -o $@ ${SPLIT_PARTS_GPUOBJS} ${DLIBS}
 
-${XFIBRES_GPU}: ${XFIBRES_GPUOBJS}
+${XFIBRES_GPU}: ${LIBBEDPOSTX_CUDA} ${XFIBRES_GPUOBJS}
 		   ${CXX} ${CXXFLAGS} ${LDFLAGS} -o $@ ${XFIBRES_GPUOBJS} ${DLIBS} -lcudart -lcuda -lcurand -lbedpostx_cuda -LCUDA -L${LIB_CUDA} -L${LIB_CUDA}/stubs
--- a/src/fdt/CUDA/diffmodels.cuh
+++ b/src/fdt/CUDA/diffmodels.cuh
@@ -68,6 +68,7 @@
 
 #include <thrust/device_vector.h>
+#include <thrust/host_vector.h>
 
 void fit_PVM_single(	//INPUT
 			const vector<ColumnVector> 	datam_vec,
 
